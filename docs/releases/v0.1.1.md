# agent-contracts v0.1.1

Release tag: `v0.1.1`

Commit: `dc76f0aeb948c7e874e2de9b7a51407ca88f74e0`

## Highlights

- Browser playground via Pyodide: https://impartshadow.github.io/agent-contracts/playground/
- Labeled corpus evaluation: `agent-contracts eval examples/eval_corpus.jsonl`
- Adoption diagnostics: `agent-contracts doctor --root .`
- Bootstrap now writes policy, adapter, CI workflow, and optional pre-commit hooks.
- Replay supports SARIF and expectation checks for incident fixtures.

## Verification

```text
PYTHONPATH=. pytest -q -> 67 passed
python3 -m build -> agent_contracts-0.1.1.tar.gz + agent_contracts-0.1.1-py3-none-any.whl
agent-contracts eval examples/eval_corpus.jsonl -> tp=3 tn=4 fp=0 fn=0 precision=1.00 recall=1.00
```

## Install

Until PyPI publishing is enabled:

```bash
pip install "agent-contracts @ git+https://github.com/impartshadow/agent-contracts.git@v0.1.1"
```

PyPI publishing is wired in GitHub Actions but still requires a repository
`PYPI_API_TOKEN` secret.
